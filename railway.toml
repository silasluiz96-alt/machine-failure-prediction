[build]
builder = "nixpacks"
buildCommand = "pip3 install -r requirements_api.txt && python3 api/model.py"

[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
